name: After deploy

on:
  workflow_run:
    workflows: [Deploy]
    types:
      - completed

jobs:
  after-deploy:
    name: Do something after deploy
    runs-on: ubuntu-latest
    steps:
      - name: Do it
        run: echo "OK!"
