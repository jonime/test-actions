name: Update staging
run-name: Deploy ${{ github.ref_name }} to staging

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  deploy-changes:
    name: Deploy changes
    uses: ./.github/workflows/deploy-changes.yml
    secrets: inherit
    with:
      env: staging
